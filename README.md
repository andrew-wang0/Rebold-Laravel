# MPC Rebold

Work in progress.

### Getting Started

Requirements: npm, [composer](https://getcomposer.org/), php >= 8.2.0

Clone this repo and run:

```bash
npm i
composer install
php artisan serve
```
